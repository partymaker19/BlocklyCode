# Плагин @mit-app-inventor/blockly-plugin-workspace-multiselect@1.0.2
# объявляет peer blockly >=11 <12, но фактически совместим с blockly 12.
# Разрешаем установку без ручных флагов.
legacy-peer-deps=true
