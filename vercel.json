{
  "buildCommand": "cd hello-world && npm run build",
  "outputDirectory": "hello-world/dist",
  "installCommand": "cd hello-world && npm install --no-audit --no-fund --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
